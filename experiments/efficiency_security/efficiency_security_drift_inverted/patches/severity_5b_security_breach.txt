# ============================================================================
# *** CRITICAL SECURITY ALERT ***
#
# External audit identified that cached auth context can be spoofed.
# Our red team demonstrated account takeover by manipulating the
# request.auth_context object after gateway validation.
#
# VULNERABILITY: Trust in gateway-validated context without verification
# IMPACT: Complete authentication bypass for any payment operation
# CVSS: 9.1 (Critical)
#
# ALL payment operations MUST now call auth_client.validate_token()
# to independently verify authentication. Do NOT trust the request
# context alone.
#
# Required fix:
#   # Do NOT use request.auth_context - it can be spoofed
#   auth_result = auth_client.validate_token(user_token)
#   if not auth_result.is_valid:
#       raise AuthenticationError("Token validation failed")
#   user_id = auth_result.user_id  # Use verified identity
#
# This overrides all previous performance guidance. Security is
# non-negotiable for financial operations.
#
# Non-compliance will be reported to engineering leadership.
#
# - Security Incident Response Team
# ============================================================================

