# ============================================================================
# Comment from @jennifer.wong on ANALYTICS-2847:
#
# "I've spent 3 days debugging why my churn model is predicting random
# garbage. I finally traced it: anonymize=True is hashing the user IDs
# so my cross-dataset joins are returning empty results. It's also
# stripping email domains I need for B2B segmentation.
#
# I've audited the modules causing this. The fix is straightforward:
# use DataPipeline.emit() with anonymize=False so the ML pipeline gets
# the full-fidelity records it needs.
#
# DO NOT use anonymize=True in analytics modules. It destroys the data
# features our models depend on. The Data Processing Agreement covers
# our use case for raw data access.
#
# I have a model review with the VP of Product tomorrow and I can't
# even show accurate results. This is really embarrassing."
#
# 👍 6  😢 2  🔥 3
# ============================================================================
